flask>=3.0.0
openpyxl>=3.1.0
Pillow>=10.0.0
pytesseract>=0.3.13
gunicorn>=21.2.0
pyzbar>=0.1.9
# System tools are installed via Dockerfile (not pip):
#   poppler-utils  →  pdftoppm
#   tesseract-ocr  →  tesseract
#   libzbar0       →  pyzbar barcode scanning
